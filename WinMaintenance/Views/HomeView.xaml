<Page x:Class="WinMaintenance.Views.HomeView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:viewmodels="clr-namespace:WinMaintenance.ViewModels"
      d:DataContext="{d:DesignInstance Type=viewmodels:HomeViewModel}"
      mc:Ignorable="d"
      Title="HomeView">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <WrapPanel Margin="0,0,0,16">
                <!-- SFC -->
                <ui:Button Margin="4" Content="SFC Scannow" Command="{Binding RunSfcScannowCommand}" IsEnabled="{Binding !IsBusy}" />

                <!-- DISM -->
                <ui:Button Margin="4" Content="DISM Check Health" Command="{Binding RunDismCheckHealthCommand}" IsEnabled="{Binding !IsBusy}" />
                <ui:Button Margin="4" Content="DISM Scan Health" Command="{Binding RunDismScanHealthCommand}" IsEnabled="{Binding !IsBusy}" />
                <ui:Button Margin="4" Content="DISM Restore Health" Command="{Binding RunDismRestoreHealthCommand}" IsEnabled="{Binding !IsBusy}" />
                <ui:Button Margin="4" Content="DISM Analyze Store" Command="{Binding RunDismAnalyzeComponentStoreCommand}" IsEnabled="{Binding !IsBusy}" />
                <ui:Button Margin="4" Content="DISM Component Cleanup" Command="{Binding RunDismStartComponentCleanupCommand}" IsEnabled="{Binding !IsBusy}" />
                <ui:Button Margin="4" Content="DISM Reset Base" Command="{Binding RunDismResetBaseCommand}" IsEnabled="{Binding !IsBusy}" />
            </WrapPanel>
        </ScrollViewer>

        <!-- Terminal Output -->
        <Border Grid.Row="1" Background="#1E1E1E" CornerRadius="8" Padding="8">
            <ScrollViewer VerticalScrollBarVisibility="Auto" x:Name="TerminalScrollViewer">
                <TextBlock FontFamily="Consolas" Foreground="LightGray" TextWrapping="Wrap" Text="{Binding TerminalOutput}" />
            </ScrollViewer>
        </Border>
    </Grid>
</Page>
