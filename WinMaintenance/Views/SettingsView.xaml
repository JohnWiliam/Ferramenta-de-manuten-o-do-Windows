<Page x:Class="WinMaintenance.Views.SettingsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:viewmodels="clr-namespace:WinMaintenance.ViewModels"
      d:DataContext="{d:DesignInstance Type=viewmodels:SettingsViewModel}"
      mc:Ignorable="d"
      Title="SettingsView">

    <Grid Margin="16">
        <StackPanel>
            <TextBlock Text="Settings" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,16"/>

            <TextBlock Text="Theme" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,8" />
            <StackPanel Orientation="Horizontal">
                <ui:Button Content="System Default" Margin="0,0,8,0" Command="{Binding ChangeThemeCommand}" CommandParameter="SystemDefault" />
                <ui:Button Content="Light" Margin="0,0,8,0" Command="{Binding ChangeThemeCommand}" CommandParameter="Light" />
                <ui:Button Content="Dark" Margin="0,0,8,0" Command="{Binding ChangeThemeCommand}" CommandParameter="Dark" />
            </StackPanel>

            <TextBlock Text="Current Theme:" Margin="0,16,0,0" />
            <TextBlock Text="{Binding CurrentTheme}" FontWeight="SemiBold" Margin="0,0,0,16" />

            <TextBlock Text="Language" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,8" />
            <StackPanel Orientation="Horizontal">
                <ui:Button Content="English" Margin="0,0,8,0" Command="{Binding ChangeLanguageCommand}" CommandParameter="en-US" />
                <ui:Button Content="Português do Brasil" Margin="0,0,8,0" Command="{Binding ChangeLanguageCommand}" CommandParameter="pt-BR" />
            </StackPanel>

            <TextBlock Text="Current Language:" Margin="0,16,0,0" />
            <TextBlock Text="{Binding CurrentLanguage}" FontWeight="SemiBold" Margin="0,0,0,16" />

            <TextBlock Text="Note: Language changes will fully apply when reopening views or restarting the application." FontStyle="Italic" Opacity="0.7" />
        </StackPanel>
    </Grid>
</Page>
